# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
*.egg
.eggs/
# Python build artefacts. These are ROOT-SCOPED on purpose: a bare `lib/`
# matches at any depth, and it was silently excluding the whole of
# `web/src/lib/` — api.ts, data.ts, theme.ts and types.ts, without which the web
# app cannot build from a fresh clone. Nothing had ever been committed from that
# directory. Same class of mistake as the blanket `*.ply` rule; see
# docs/repo_review.md finding 2.
/build/
/dist/
/develop-eggs/
/downloads/
/eggs/
/lib/
/lib64/
/parts/
/sdist/
/var/
/wheels/
share/python-wheels/
MANIFEST
pip-log.txt
pip-delete-this-directory.txt

# Virtual environments
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/
.python-version

# Testing / coverage
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Env / secrets
.env
.env.*
!.env.example
*.pem
*.key

# Logs
*.log
logs/
lightning_logs/
wandb/
runs/
tb_logs/
mlruns/

# Model weights / checkpoints
*.pt
*.pth
*.ckpt
*.safetensors
*.bin
*.onnx
*.h5
*.pkl
*.pickle
checkpoints/
weights/
pretrained/
models/
hf_cache/
.cache/

# Data / outputs
output/
outputs/
# Per-capture runs written with --output_dir output_<name>; each holds a ~93 MB
# predictions.npz and regenerates from inputs/<name> in a couple of minutes.
output_*/
results/
data/
datasets/

# Scratch: stagerun experiment runs (gigabytes) and ad-hoc mesh comparisons
work/
compare_recon/
compare_ghost/
temp_output_compare_recon/
*.zip


# 3D assets (large)
#
# The web app ships precomputed sample meshes and they must be committed, or a
# fresh clone renders an empty viewer with no error. The blanket rule below hid
# them once already; the negation has to come after the rule to take effect.
*.ply
*.obj
*.glb
*.gltf
*.fbx
*.stl
*.usd
*.usdz
*.blend
*.blend1

# ...except the web app's shipped samples, which are the demo.
!web/public/samples/**

# Media
*.mp4
*.avi
*.mov
*.mkv

# Compiled
*.o
*.a
*.dll
*.dylib

# test folder
test/

# Obsidian vault — per-machine UI state (notes + plugin config stay tracked)
obsidian/
CLAUDE.md
